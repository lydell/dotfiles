# Mod2+Mod4 is Super.

"gnome-session-quit --power-off"
    Shift+Mod2+Mod4 + q

"gnome-session-quit --logout"
    Shift+Mod2+Mod4 + l

"winresize left"
    Mod2+Mod4 + n

"winresize max"
    Mod2+Mod4 + i

"winresize right"
    Mod2+Mod4 + s

# For QWERTY.
"winresize right"
    Mod2+Mod4 + j

"gnome-terminal & sleep 0.1 && winresize left"
    Mod2+Mod4 + Return

"wmctrl -c :ACTIVE:"
    Mod2+Mod4 + q

"wmctrl -a terminal || (gnome-terminal & sleep 0.1 && winresize left)"
    Mod2+Mod4 + h

"wmctrl -a gvim || (gvim && winresize right)"
    Mod2+Mod4 + t

"wmctrl -a abrowser || wmctrl -a firefox || firefox"
    Mod2+Mod4 + r

"wmctrl -a chromium || wmctrl -a nightly || chromium-browser || firefox-trunk"
    Mod2+Mod4 + o
