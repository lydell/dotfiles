{
  "private": true,
  "scripts": {
    "build:tabs.scss": "node-sass tabs.scss tabs.css",
    "watch:tabs.scss": "npm run build:tabs.scss && chokidar tabs.scss -c 'npm run build:tabs.scss'"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "node-sass": "^3.4.2"
  }
}
