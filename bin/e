#!/usr/bin/env bash

cmd="rg --hidden"

seenP=''
for arg in "$@"; do
  if test "$arg" == '-p'; then
    seenP='true'
    break
  fi
done

if test -n "$seenP"; then
  $cmd "$@" | less -RFX
else
  $cmd "$@"
fi
